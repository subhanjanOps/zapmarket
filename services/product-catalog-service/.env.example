# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=zapuser
DB_PASSWORD=zappass123
DB_NAME=productcatalog

# JWT (used for local validation fallback)
JWT_SECRET_KEY=your-secret-key-change-in-production

# Redis
REDIS_URL=localhost:6379

# Elasticsearch
ES_URL=http://localhost:9200

# Service ports
HTTP_PORT=8081
GRPC_PORT=50052

# Environment
APP_ENV=development

# Downstream services
AUTH_SERVICE_ADDR=localhost:50051
