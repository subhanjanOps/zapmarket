# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=zapuser
DB_PASSWORD=zappass123
DB_NAME=payment

# Service ports
HTTP_PORT=8083
GRPC_PORT=50054

# Environment
APP_ENV=development

# Payment gateway webhook signature secret (HMAC-SHA256 over the raw body).
# Placeholder only — never commit a real secret.
PAYMENT_WEBHOOK_SECRET=your-webhook-secret-change-in-production
